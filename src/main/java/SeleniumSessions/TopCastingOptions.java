package SeleniumSessions;

import org.openqa.selenium.Capabilities;
import org.openqa.selenium.SearchContext;
import org.openqa.selenium.WebDriver;
import org.openqa.selenium.chrome.ChromeDriver;
import org.openqa.selenium.chromium.ChromiumDriver;
import org.openqa.selenium.remote.RemoteWebDriver;

public class TopCastingOptions {

    // WebDriver driver = new ChromeDriver(); (recommended)

   // SearchContext driver = new ChromeDriver(); (not recommended)

    // RemoteWebDriver driver = new ChromeDriver(); (recommended)

    // ChromiumDriver driver = new Chromedriver(); (not recommended)

    // WebDriver driver = new RemoteWebDriver(); (recommended)

    // ChromeDriver driver = new Chromedriver(); (not recommended)

    // SearchContext driver = new RemoteWebDriver(Url , Capabilities); // (not recommended)

}
