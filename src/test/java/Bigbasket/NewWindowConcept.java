package Bigbasket;

public class NewWindowConcept {

    public static void main(String[] args) {

        // driver.switchTo().newWindow(WindowType.TAB) --> It creates new Tab in the browser and auto switch to the new tab

        // driver.switchTo().newWindow(WindowType.WINDOW) --> It creates new Window browser and auto switch to the new window browser
    }
}
